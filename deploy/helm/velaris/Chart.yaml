apiVersion: v2
name: helix
description: HELIX BPM platform — case-service, engine, worker, Postgres, Valkey
type: application
version: 0.1.0
appVersion: "0.1.0"
dependencies:
  - name: postgresql
    version: "15.5.14"
    repository: "https://charts.bitnami.com/bitnami"
    condition: postgresql.enabled
  - name: valkey
    version: "4.0.2"
    repository: "https://charts.bitnami.com/bitnami"
    condition: valkey.enabled
