{{- if .Values.caseService.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "helix.caseServiceName" . }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "helix.caseServiceName" . }}
  minReplicas: {{ .Values.caseService.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.caseService.autoscaling.maxReplicas }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.caseService.autoscaling.targetCPUUtilizationPercentage }}
{{- end }}
